apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

# Example namespace. Infrastructure provisions it and the external dependencies.
namespace: moongcheap-ai-dev

labels:
  - pairs:
      app.kubernetes.io/instance: moongcheap-ai-dev
    includeSelectors: false

# Keep the image/address/artifact placeholders and suspend=true until configured.
