# 판매자 수요 분석 HTTP 진입 계층(`moongcheap_ai.seller_analysis.api`)에만 필요하다.
#
# 계산 계층(`bid_guide.py`)과 계약 검증은 표준 라이브러리만 쓰므로 이 파일 없이도 돈다.
# `tests/seller_analysis/test_api_http.py` 는 FastAPI 가 없으면 건너뛰되
# **건너뛴다는 사실이 보이게** 한다 — CI 에서 HTTP 계약을 실제로 검증하려면 이 파일을 설치한다.
#
#   pip install -r requirements-api.txt
#   SELLER_ANALYSIS_INTERNAL_KEY=... \
#     python -m uvicorn moongcheap_ai.seller_analysis.api:create_app --factory --port 8081
fastapi==0.115.6
uvicorn==0.34.0
httpx==0.28.1          # TestClient 가 요구한다
