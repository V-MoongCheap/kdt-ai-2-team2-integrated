"""영역 2 오프라인 평가 도구.

⛔ 운영 경로(`api` · `bid_guide`)는 이 패키지를 부르지 않는다.
   `demand_clustering.evaluation` 과 같은 경계다.

- `eval_set` — 고정 평가셋 100건 생성. **구현을 부르지 않는다.**
  정답을 구현이 만들면 채점이 순환한다
- `runner` — 그 평가셋으로 현재 구현을 채점
"""
